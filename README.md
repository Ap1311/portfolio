# Portfolio

A modern, responsive single-page portfolio template built with plain HTML and CSS.

## Run locally

Open `index.html` directly in your browser, or run a static server:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
